---
name: bilibili-fav-organizer
description: 批量整理 B站(Bilibili)收藏夹：读取全部收藏夹视频、按「标题+标签+封面」逐条分类（本地视觉模型优先、文本子代理兜底）、批量移动到对应收藏夹、对已失效视频取消收藏。只要用户提到整理/分类/清理 B站收藏夹、bilibili favorites、收藏夹搬家、失效视频清理、收藏夹太多要归类，即使没说"skill"也应触发本技能。内置 412 风控应对、本地视觉分类子模型编排、文本子代理兜底、API 批量移动方法论（含静默失败陷阱与抽干验证）、GUI 兜底流程。
---

# B站收藏夹批量整理（方法论版）

把用户混乱的 B 站收藏夹整理成按用途分类的文件夹。整体分四个阶段，顺序执行，**全程不要询问用户"是否继续"，只管执行**：

0. **登录（阶段零）**：打开干净浏览器 → B 站登录页 → 用户扫码 → 确认登录态并拿到账号标识（mid）。
1. **盘点（只读）**：通过平台公开接口读取全部收藏夹和视频元数据，本地备份。
2. **分类（离线）**：失效视频直接取消收藏；活视频按「标题 + 标签 + 封面」逐条判断（本地视觉模型优先，文本子代理兜底，主模型最终兜底）。
3. **执行（写入）**：优先用平台接口批量移动/取消收藏，**以抽干验证为准**；接口不通时退回 GUI 逐条流程。

> **本发布版说明**：本技能为**方法论文档**，不含任何可执行代码。涉及接口/脚本的部分均以伪代码示意（见 `pseudocode/`），真实端点、请求形态、页面选择器请按平台官方文档现场核对。详见 `README.md` 与 `DISCLAIMER.md`。

## 阶段零：登录（首次必须，之后沿用登录态文件）

目标：从干净浏览器走到"已登录且知道 mid"的状态。两步：

1. `open`：导航到登录页。输出 `NEED_LOGIN` 后，**必须截图展示二维码给用户扫码**。若浏览器已登录，直接输出 `ALREADY_LOGGED` 并记录 mid，跳过扫码。
2. `wait`：轮询登录态（默认最多 120 秒）。成功后写 `login.json`（`{mid, name}`），输出 `LOGIN_OK`。

要点：

- **登录走干净浏览器扫码**（硬性纪律）：不要动用户的 Edge/Chrome 等现有浏览器，不提取其 cookie。
- mid 来源：账号信息接口返回的 mid；cookie 中账号 ID 字段兜底。
- 后续所有步骤的 mid / 登录态都从 `login.json` 读，登录只需做一次（除非 cookie 过期）。
- 登录后跳到收藏夹页面（GUI 流程起点）。

## 阶段一：盘点（读取全部数据）

用浏览器内直接访问平台接口的方式读取（浏览器自动携带登录态 cookie）：

- 收藏夹列表接口：按账号标识读取全部已创建的收藏夹（id/名称/条数/属性）。
- 收藏夹内容接口：按收藏夹 id 分页读取视频（每页 N 条，游标分页）。

关键坑（方法论原则，详见 `docs/api-methodology.md`）：

- **分页必须循环"是否有更多"标记**，不能因为某页返回条数不足就停（第 1 页就可能不满但仍有下一页）。
- **失效视频标记**（属性字段非 0，标题常为"已失效视频"）→ 标记为 DEAD，后续取消收藏。
- 每读一页先备份到本地 JSONL：`{收藏夹id, 收藏夹名, aid, bvid, 类型, 标题, UP主, 失效标记, 封面, 简介}`。
- **全程限速**：每页请求间隔 1.1~1.8 秒（加随机抖动），开始前先访问一次平台首页预热。连续快速请求会触发 IP 级风控。

## 硬性纪律（违反即失败）

- **不要问用户要不要继续、不要问其他问题，只管执行。** 遇到可恢复的错误就自行重试/降速。
- **必须使用视频标题进行分类判断，禁止偷懒**（不许不看标题就乱丢分类，不许全部扔进一个兜底文件夹）。
- 分类风格**按用途**，并与用户已有收藏夹的命名风格保持一致（先看一遍现有收藏夹再定类目）。
- 读取收藏夹时**必须读完所有收藏夹的所有分页**，用户收藏夹往往不止三五个。
- **不要动用户的现有浏览器**，不提取其 cookie；登录走干净浏览器扫码。

## 阶段二：分类（主模型定类目 → 模型/子代理分类，封面视觉兜底）

流程：

0. **拉标签（第一信号）**：按视频标识逐条调用平台标签接口（若平台提供），输出 `tags.jsonl`，断点续传 + 限速 + 风控退避。无标签接口则跳过本步。
1. **失效视频**（失效标记非 0）直接归 DEAD，不进分类。
2. **主模型定类目（纯文本）**——**不写死固定类目表**：
   - **有自定义收藏夹** → 类目 = 现有收藏夹名，主模型为每个生成简短定义（该夹名对应什么内容）；
   - **只有默认收藏夹** → 从标签数据读默认夹前一批视频的标签，主模型按标签聚类 → 生成建议类目（名称 + 定义）；
   - 类目数控制在 8~20，**类目名优先用收藏夹名原样**（分类结果直接映射现有夹）；输出 `categories.json` + 兜底项 `{"name":"未分类"}`。
   - **类目完全自适应**：分类名称不限于任何预置清单，由主模型按"现有收藏夹名 / 标签聚类"动态生成，每个账号的类目体系都按它的实际内容定制。
3. **本地模型可用性检查（降级链关键）**：
   - **有本地视觉模型**（本机 MCP/模型服务可用）→ 走视觉分类（封面+标题+标签，`vision_classify` 一类工具）；
   - **无本地视觉模型** → 走**文本子代理**按标题+标签分类（模板见 `docs/subagent-classify.md`）；
   - **本地模型反复卡死**（连续清队列/重启/降批次后仍持续超时或报错）→ **放弃本地视觉，切换文本子代理**按标题+标签分类（模板见 `docs/subagent-classify.md`）；
   - **子代理超时/卡死**（长时间无产出）→ 停止拉起，**主 Agent 直接分类**（主模型按标题+标签+UP 主逐条判断，上下文充裕，速度更快更稳）。
   - 判断顺序固定：**本地视觉 → 反复卡死则文本子代理 → 子代理卡死则主模型**。
4. **分类输入 = 标题 + 标签拼接**：`标题 | 标签：tag1,tag2,...`（无标签则只传标题）。**不做关键词→类目映射**（关键词正则无语义理解，易误分类）。
5. **图片策略（省封面下载 + 省视觉推理）**：
   - **有明确标签的条目：不传封面图**——纯文本按类目语义分类，快且省显存；
   - **标签缺失 / 泛标签**（自制/vlog/生活/日常 等无法定类）→ 下载封面走视觉兜底（封面 CDN 免登录，限速，按标识缓存；404 则留空仅文本）。
6. **切块与调用**：按批串行调用，每批 ≤8 条，类目传动态生成的 categories，规则传补充映射（如特定类型 UP 主的固定归类）。**结果按批落盘**（JSONL），可断点续跑。
7. **合并铁律**：aid 集合与输入相等、类目都在枚举内、行数相等，先校验后合并；校验失败的批次**补封面重排**（先无图判断失败的，加图再试一次），仍败则回退文本子代理。
8. **未分类二次筛选（归档兜底，必做）**：分类完成后，对全部"未分类"条目做**二次筛选**——主模型重新看一遍标题+标签+UP 主（不再走视觉），尝试归入现有类目。**仍无法判断的条目**：按其标题/内容主题**命名并创建新收藏夹**归档（如"其他·归档"），不要让它们散落在旧夹里；新夹加入 categories 并映射进执行计划的 create 清单。仅当新夹也明显不合适（个别孤例）才保留在原夹并列入最终报告。

### categories 生成提示词（主模型用，纯文本）

```
你是 Bilibili 收藏夹整理任务的类目规划。根据用户现有收藏夹 / 视频标签，输出分类类目 JSON 数组。

输入1（现有收藏夹名）：<收藏夹名列表，或"只有默认收藏夹">
输入2（视频标签统计）：<标签频次清单，或前一批标签>

规则：
1. 类目数控制在 8~20 个。
2. 有自定义收藏夹时：类目名 = 现有收藏夹名（原样），为每个生成 definition（该夹名代表什么内容，10 字内）。
3. 只有默认收藏夹时：按标签聚类生成新类目名（简短直观，与用户收藏夹命名风格一致），为每个生成 definition。
4. 类目要按用途区分，避免过细（同类标签合并）。
5. 最后加一条 {"name":"未分类","definition":"标签与标题都无法判断时使用"}。

输出（只输出 JSON 数组，不要其他文字）：
[{"name":"游戏·其他","definition":"游戏攻略/实况/资讯"}, ...]
```

### 补充规则（作为数据引用）

- **Steam 促销资讯类 UP 主** 归"游戏·其他"（此类 UP 主以转发促销/史低/喜加一资讯为主）。
- 标题含明确游戏名的按游戏归类（战地系列→战地，Minecraft→我的世界，其余→游戏·其他）。
- 分类匹配文本只用 `标题 + UP主名`；**不要拼简介**（简介噪声会导致误分类）。

### 通用类目示例（21 类，仅当用户无收藏夹且主模型类目生成失败时的备选）

鱼·水族（鱼缸/龟缸/水草/造景/观赏虾）；做菜·美食（菜谱/烹饪/探店/吃播）；歌单·音乐（歌曲/MV/纯音乐/翻唱/演奏）；PCB·电路设计（PCB/EDA/电源设计/焊接）；工控·PLC（PLC/博途/西门子工控/工厂自动化/电工）；学习·学科（学科课程/考试备考/升学志愿）；编程·AI（编程语言/开发工具/大模型/嵌入式软件/网络安全）；我的世界（MC 模组/红石/整合包）；战地系列（战地系列游戏）；动漫·番剧（番剧/动画解说/动漫剪辑）；COS·漫展·直播（cosplay/漫展/直播回放/写真拍摄）；军事·武器（武器装备/军事新闻/战史/军工）；无人机·航模（无人机/FPV/航模/飞控）；硬件·数码（手机电脑外设/装机/评测）；健康·医学科普（医学科普/疾病/急救/心理健康）；法律·社会（法律案件/律师说法/社会新闻/维权）；时政·历史（时政评论/历史科普/国际关系/键政）；游戏·其他（未覆盖游戏的攻略实况资讯/Steam 促销/行业评论）；软件·工具·壁纸（通用软件教程/效率工具/壁纸）；影视·解说（影视解说/电影剪辑）；生活·旅行·妙招（生活技巧/旅行/妙招）。

> 此清单与 `docs/subagent-classify.md` 子代理模板共用同一套类目；模板另附"未分类"条目（编号 22）。

## 阶段三：执行（接口批量优先，GUI 兜底）

### 前置①：主模型生成执行计划（分类结果 → 收藏夹规划）

分类完成后，主模型读现有收藏夹 + 分类统计 + 类目定义，生成执行计划：

```json
{
  "reuse": { "游戏·其他": 123, "歌单·音乐": 456 },   // 分类 → 现有收藏夹 id（类目名与夹名精确对应）
  "create": ["编程·AI", "影视·解说"],                  // 需要新建的收藏夹（现有夹没有的类目，含未分类归档夹）
  "delete": [789, 790]                                // 分类后未映射且为空的收藏夹（仅空夹可删）
}
```

规划规则（主模型判断，纯文本）：
- **reuse**：类目名与现有收藏夹名**精确匹配**（动态类目阶段已保证类目名=夹名原样，天然对齐）；不要字符串包含猜测。
- **create**：现有夹没有的类目才新建——含二次筛选后为"未分类"条目命名的新归档夹。
- **delete**：分类映射后**未被任何 reuse/create 使用**且**为空**的收藏夹。**非空夹绝不列入 delete**（删除非空夹可能导致视频消失）。
- DEAD 不建夹不映射；**未分类**经二次筛选后应进入新归档夹。

### 前置②：人工确认后执行

**执行计划必须展示给用户确认**（列出新建/删除清单），用户确认后才执行创建/删除。执行顺序：先创建后删除。

### 首选：接口批量执行

在已登录页面上下文里发 POST（同步 XHR + 协议相对 URL + 现读 CSRF 令牌，形态见方法论文档）：

- **移动**：`src_media_id, tar_media_id, mid, platform=web, resources=<aid>:<type>,<aid>:<type>,...`（**逗号分隔单参数，一次 ≤10 条**）。
- **失效视频取消收藏**：`media_id=<源夹>, platform=web, resources=...` 同上。
- **三条铁律（防静默失败）**：
  1. **resources 绝不能重复参数名**——服务端只认第一个，其余静默丢弃且返回成功码（曾导致大量操作无声失效）。
  2. **type 后缀取记录的 type 字段**（2=稿件、24=剧集条目…），写错同样静默无效。
  3. **成功码 ≠ 生效**：执行完必须把源收藏夹用列表接口抽干到无更多复核，把残留清单重新入队补跑，循环直到抽干为 0。条数是缓存值，不可信。
- 限速（1.0~1.6s/请求 + 每 40 请求暂停 3 秒），每批把成功的 aid 追加进日志实现断点续跑。
- 同一视频在多个收藏夹重复存在时：移动到目标夹会把源夹副本移除、目标夹保留已有条目，可安全清重。

### 兜底：GUI 逐条流程

接口写入被风控卡死时，改用收藏夹页面逐个处理视频（核心流程与"位置刷新"认知见 `docs/gui-flow.md`）：

1. 打开第一个视频的"更多选项"菜单，等下拉出现。
2. 若是已失效视频 → 点"取消收藏"；否则点"移动至"，等弹框。
3. 按该视频标题对应的分类，在弹框中选对应收藏夹。
4. 点"确定"。

**位置刷新关键认知**：处理完第 1 个视频后，第 2 个会顶到第 1 个的位置。因此对页面上第 N 个视频，操作的第一步仍是"第一个视频的更多选项按钮"，只是分类目标换成第 N 个视频的标题。收到一页后一次性生成整页操作序列，统一执行；执行完重新获取页面，重复直到该收藏夹再无视频。

**每步之间留 1~2 秒等待**：既等 DOM 刷新，也天然规避风控。每个操作后验证结果（菜单消失/弹框出现/视频从列表消失），失败则重试该条。

## 降级链与备份机制（方法论）

分类/执行的每一步都要有**明确降级路径**，遇到卡死不要原地重试超过 2 次，直接降级。按成本从低到高：无图判断 → 封面视觉 → 主模型直接分类；接口不通 → 清理重启 → GUI 兜底。

### 本地模型清理与重启（方法论）

- **MCP 工具调用有默认超时**，批量带图推理易超时被掐断。改法：客户端 MCP 配置加长超时（改完**必须重启客户端**才生效）；同时封面压缩到小尺寸（单图推理时间可差一个数量级）。
- **掐断 ≠ 取消**：客户端超时掐断后，请求可能仍在本地模型队列里继续跑 → 连环超时、显存堆积。**遇到连续 2 次超时立即清队列**（终止模型相关进程全家桶，进程名要全防孤儿），冷却后重启服务再继续。
- **显存被掐断请求堆满时**：观察 GPU 利用率持续高位即队列堆积，先清进程再跑；每批分类前可查模型服务状态估算单批耗时，单批超时率高时主动降批次。
- **分页读取偶发中止**：读取请求要给中止异常包重试 + 冷却（脚本内 goto 重试）。
- **本地模型反复卡死 → 切换文本子代理（降级闭环）**：本地视觉模型连续 2 次清队列/重启/降批次后仍持续超时或报错（判定"反复卡死"）→ **放弃本地视觉，切换文本子代理**按标题+标签分类（模板见 `docs/subagent-classify.md`）；子代理连续 2 次卡死 → 主 Agent 直接分类（下一节）。**完整降级顺序固定：本地视觉 → 反复卡死则文本子代理 → 子代理卡死则主模型**。

### 子代理分类：卡死检测 + 主模型兜底

**纯文本分类的备份机制**：本地视觉模型不可用时，先拉起文本子代理并行分类；**子代理连续 2 次卡死（running 但长时间零产出）则停止拉起，改由主模型直接分类**——主模型按标题+标签+UP 主逐条判断，上下文充裕（准确度考虑控制在实际可处理范围内），速度通常比子代理更快更稳。

- **子代理卡死特征**：状态运行中但结果文件长时间未落盘、会话目录时间戳停在创建时刻。处理：停止 → 删除会话残留 → 重新拉起一次；**再卡则不再拉起，主模型直接分类**（读分批文件逐条判断 → 写落盘文件 → 执行，断点续传）。
- 子代理防谎报：必须要求写完后用行数统计 + 读首尾几行自证；主模型合并前独立校验 aid 集合/类目枚举/行数，任一校验失败重跑该批。
- 子代理切块 ≤300 条/份、并行 ≤4 个；输入每行 `aid<TAB>标题<TAB>标签<TAB>UP主`。

### 接口写入注意事项（方法论）

- **写操作必须走浏览器页面上下文**（脚本内 XHR 或 GUI 点击）：脱离浏览器直接发请求缺登录态 cookie（如 HttpOnly 字段），会返回未登录错误。**先做一个最小写操作冒烟测试**（如删除一个已确认空的收藏夹），通了再批量。
- **执行日志误记成功陷阱**：同一 aid 在多个夹重复时，某次移动静默失败但被日志记成功，断点续传会跳过导致残留清不掉。抽干发现残留后，**强制补跑**（忽略历史成功记录）重试。
- **未分类条目卡源夹**：跳过的未分类条目会留在待删夹里，删夹前必须补归类移走（主模型按标题快速判断即可，不必再走视觉）。
- **抽干验证只对待删夹做清空判定**：目标夹内容被抽进残留清单属正常（条数增长不是残留）。
- **轮次节奏**：每轮限制请求数 + 轮间冷却，可全程不触发风控。

## 安全、隐私与任务结束清理

### 隐私边界

- **登录只走干净浏览器扫码**，不碰用户现有浏览器，不提取/复制其 cookie。
- **cookie 只存在于浏览器内存会话**：只读 CSRF 单值（仅写操作前一刻用），**从不把完整 cookie 落盘**。
- 本地落盘数据（登录信息、收藏元数据、分类结果）含账号关联信息，**不要放入 git 仓库、网盘自动同步、云盘上传**等任何可能外发的通道。
- 封面/标签分类走**本地视觉模型**（若有），图片不出本机；子代理只收标题+标签+UP 主文本，不涉 cookie/账号。

### 任务结束清理

任务全部完成（或中断终止）后：

1. **关闭浏览器标签**（含收藏夹页、接口页、登录页）。
2. **清浏览器状态**：登录态/cookie/缓存/历史随会话保留在浏览器进程里——关闭全部标签，并提示用户可在浏览器面板手动清除站点数据。
3. **删除临时文件**（本次任务产生的、非存档用途的）：cookie 调试文件、临时压图目录、中间分批目录；保留分类结果与盘点数据作备份。
4. **本地模型收尾**：视觉模型卸载释放显存（若有）。
5. 汇总报告里说明：清理了哪些临时文件、保留了哪些备份、浏览器状态是否已清。

### 风险自查清单

- 是否有步骤会把完整 cookie 写盘？→ 禁止，只允许写操作前读单值。
- 数据目录是否在 git/网盘同步路径下？→ 是则提示用户排除，或改用临时目录。
- 截图展示二维码后是否及时关闭登录页？→ 是。
- 子代理提示词是否夹带 cookie/账号之外的敏感信息？→ 只传标题+标签+UP 主。

## 风控管理（贯穿所有阶段）

平台反爬风控（如 HTTP 412）是 **IP 级**的：重置指纹没用，换登录态也没用。应对：

- **预防**：任何接口连读都限速（1.1s+ 抖动），先访问首页预热；GUI 操作天然较慢，一般不触发，但弹窗连点也不要过快。
- **触发后**：立即停止请求，冷却等待（首次退避 12 秒，每次重试递增 6 秒，最多重试 4 次）；大规模触发需要分钟级冷却后再恢复。读取是只带 cookie 的 GET，冷却后一般自动恢复。
- 写入阶段如果走接口且被风控，转 GUI 流程执行。

## 端到端执行顺序（产物链）

```
登录（扫码）                              →  login.json（账号标识）
收藏夹列表                                →  folders_raw.json（全部收藏夹）
盘点（按夹读取，断点续传）                →  fav_all.jsonl
拉标签（可选，第一信号）                  →  tags.jsonl
主模型定类目（收藏夹名/标签聚类）         →  categories.json
分类（无图优先/封面兜底/子代理/主模型）    →  classified.json（{aid, cat}）
主模型规划（reuse/create/空夹 delete）     →  执行计划（人工确认）
执行（建夹/删空夹）                       →  folders.json（类目→收藏夹映射）
批量移动 + 失效取消（断点续传）           →  执行日志
抽干复核源夹，残留回灌补跑                →  残留清单
（接口不通时：GUI 逐条兜底）
```

- 数据目录约定：统一用同一目录（`<数据目录>`），所有中间文件都在该目录下，脚本间通过文件衔接。
- 断点续跑：盘点按已读收藏夹跳过；执行按执行日志跳过已成功项；抽干验证按页码断点续页。中断后重跑同一流程即可续。
- 首次执行必须走阶段零登录；登录态文件存在且有效时可跳过。
- **执行计划是人工确认关口**：展示新建/删除清单给用户确认后才执行。

## 参考文件

- `docs/api-methodology.md` — 接口方法论：分页/失效标记/批量写入三铁律/抽干验证原则。
- `docs/subagent-classify.md` — 文本子代理分类方法论 + 提示词模板 + 防谎报校验。
- `docs/gui-flow.md` — GUI 兜底流程方法论（位置刷新认知 + 节奏）。
- `pseudocode/` — 各步骤的伪代码示意（不可执行）。
- `README.md` / `DISCLAIMER.md` — 使用说明与免责声明。
