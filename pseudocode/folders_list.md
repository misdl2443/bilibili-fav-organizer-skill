# folders_list（阶段一：收藏夹列表）

> 伪代码（不可执行），仅示意流程。

## 职责
读取账号创建的全部收藏夹列表（id/名称/条数/属性），供盘点与规划使用。

## 流程
1. 从 `login.json` 读账号标识（mid）
2. 调用"收藏夹列表"接口（只读 GET，浏览器上下文自动带登录态）
3. 返回异常（风控）→ 冷却重试（最多 3 次）
4. 成功 → 写 `folders_raw.json`：每条含 id / 名称 / 条数 / 属性

## 关键点
- 只读 GET，无写操作。
- 包含空收藏夹也要列出（后续规划 delete 用）。
